export const metadata = { title: 'App', description: 'Generated with spoonfeed' };

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
