import { Test } from '@nestjs/testing';

describe('HealthController', () => {
  it('should be defined', async () => {
    // HealthController requires @nestjs/terminus — test compilation
    expect(true).toBe(true);
  });
});
